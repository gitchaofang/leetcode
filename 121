class Solution {
public:
    int maxProfit(vector<int>& prices) {
        int res = 0;
        int low = INT_MAX;
        for(const int& d: prices){
            if(d < low) low = d;
            else res = std::max(d-low,res);
        }
        return res;
    }
};
